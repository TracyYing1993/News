<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/bg_color"
    tools:context=".HomeActivity">

    <LinearLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="?attr/bg_color"
        android:orientation="vertical"/>


    <RadioGroup
        android:id="@+id/radioTab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="?attr/home_tab_bg_color">
        <RadioButton
            android:id="@+id/radioBtn_news"
            android:checked="true"
            android:drawableTop="@drawable/news_selector"
            style="@style/myradiobutton"
            android:text="新闻"
            android:drawablePadding="2dp"
            android:textColor="@color/textColor"/>
        <RadioButton
            android:id="@+id/radioBtn_video"
            style="@style/myradiobutton"
            android:drawableTop="@drawable/video_selector"
            android:text="视频"
            android:textColor="#fff"
            android:drawablePadding="2dp"/>
        <RadioButton
            android:id="@+id/radioBtn_self"
            style="@style/myradiobutton"
            android:drawableTop="@drawable/me_selector"
            android:text="我的"
            android:textColor="#fff"
            android:drawablePadding="2dp"/>

    </RadioGroup>
</LinearLayout>
